## Example parameters file for oc process --param-file
## Usage:
##   # Ensure both namespaces exist
##   oc new-project llama-stack-demo || true
##   oc new-project special-payment-project || true
##   # Process and apply
##   oc process -f openshift_mcp_server/template.yaml --param-file=openshift_mcp_server/.env.example | oc apply -f -
##
## Adjust values as needed and copy to .env (or another filename) before applying.

DEPLOY_NAMESPACE=llama-stack-demo
TARGET_NAMESPACE=special-payment-project
APP_NAME=kubernetes-mcp-server
IMAGE=quay.io/manusa/kubernetes_mcp_server
IMAGE_TAG=latest
LOG_LEVEL=2
READ_ONLY=true

CPU_REQUEST=50m
MEMORY_REQUEST=64Mi
CPU_LIMIT=200m
MEMORY_LIMIT=256Mi

